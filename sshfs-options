#/bin/bash

sshfs = $(which sshfs)

SERVER = $1

MOUNTPINT = $2

sshfs -C \
		-o allow_other \
		-o idmap=user \
		-o reconnect \
		-o transform_symlinks \
		-o follow_symlinks \
		$SERVER \
		$MOUNTPOINT
